## Table

### Table 13-03-2026

```
**🔴 ลบตาราง**
1. RequisitionParticipantPerson
2. RequisitionParticipantPlace
3. RequisitionParticipantJuristic
4. RequisitionDraft
5. MasterObjectiveType
6. MasterPlaceType
```

```
**🟢 เพิ่มตาราง**
1. RequisitionParticipantAddress (เพิ่มแล้ว)
```

<style scoped>
table {
  font-size: 13px;
}
</style>

| ลำดับ | Field_name | Data type | Description |
|---|---|---|---|
| 1 | Id | int | รหัสอ้างอิงที่ใช้ในระบบ |
| 2 | CreateBy | int | ผู้สร้าง อ้างอิง SystemUser.Id |
| 3 | CreateOn | datetime | วันที่สร้าง |
| 4 | UpdateBy | int | ผู้แก้ไข อ้างอิง SystemUser.Id |
| 5 | UpdateOn | datetime | วันที่แก้ไข |
| 6 | RequisitionId | int | คำขอ อ้างอิง Requisition.Id |
| 7 | RequisitionParticipantId | int | บุคคลหรือหน่วยงานที่เกี่ยวข้องกับคำขอ อ้างอิง RequisitionParticipant.Id |
| 8 | HouseCode | nvarchar(255) | เลขรหัสประจำบ้าน |
| 9 | HouseNo | nvarchar(255) | เลขที่ |
| 10 | VillageNo | nvarchar(255) | หมู่ที่ |
| 11 | VillageName | nvarchar(255) | อาคาร/หมู่บ้าน |
| 12 | Lane | nvarchar(255) | ตรอก/ซอย |
| 13 | Street | nvarchar(255) | ถนน |
| 14 | ProvinceId | int | จังหวัด อ้างอิง MasterProvince.Id |
| 15 | ProvinceName | nvarchar(50) | ชื่อจังหวัด อ้างอิง MasterProvince.NameThai |
| 16 | AmphurId | int | อำเภอ/เขต อ้างอิง MasterAmphur.Id |
| 17 | AmphurName | nvarchar(50) | ชื่ออำเภอ/เขต อ้างอิง MasterAmphur.NameThai |
| 18 | TambonId | int | ตำบล/แขวง อ้างอิง MasterTambon.Id |
| 19 | TambonName | nvarchar(50) | ชื่อตำบล/แขวง อ้างอิง MasterTambon.NameThai |
| 20 | Postcode | nvarchar(10) | รหัสไปรษณีย์ |
| 21 | FullAddress | nvarchar(MAX) | ที่อยู่แบบเต็ม |
| 22 | Phone | nvarchar(10) | เบอร์โทรศัพท์ |
| 23 | Fax | nvarchar(10) | โทรสาร |
| 24 | Email | nvarchar(255) | อีเมล |

```
**🟡 RequisitionParticipant**
1️⃣ เพิ่มฟิลด์
- 1 PrefixId
- 2 FirstName
- 3 Lastname
- 4 JuristicName
- 5 FullAddress
- 6 PlaceId
2️⃣ ลบฟิลด์
- HouseCode
- HouseNo
- VillageNo
- VillageName
- Lane
- Street
- ProvinceId
- DistrictId
- SubDistrictId
- Postcode
```

```
**🟡 แก้ตาราง LicenseParticipant**
1️⃣ เพิ่มฟิลด์
- 1 IdentityType (char 1=บุคคลฯ, 2=สถานที่, 3=นิติบุคคล)
- 2 FullName
```

```
**🟡 แก้ตาราง Requisition**
1️⃣ เพิ่มฟิลด์
- 1 SubmitDate [วันที่ยื่น]
- 2 SubmitBy [ผู้ยื่น]
- 3 ReceiveNo [เลขที่รับ]
- 4 ReceiveDate [วันที่รับ]
- 5 ReceiveBy [ผู้รับ]
2️⃣ ลบฟิลด์
- FilingNo (เลขที่รับ [เปลี่ยนไปใช้ ReceiveNo])
- FillingDate (ตอนนี้ใช้ Return ค่าตอน Search จะกระทบ Search result [เปลี่ยนไปใช้ SubmitDate])
- FillingBy (ผู้รับ [เปลี่ยนไปใช้ ReceiveBy])
- NarcoticDrugName
```

```
**🟡 แก้ตาราง MappingPlacePlaceAddress**
1️⃣ เพิ่มฟิลด์
- 1 VillageName
- 2 ProvinceName
- 3 AumphurId
- 4 AumphurName
- 5 TambonId
- 6 TambonName
- 7 FullAddress
2️⃣ ลบฟิลด์
- DistrictId
- SubDistrictId
```

## Amphur, Tambon 
### Amphur 

```
SELECT 
    s.name AS schema_name,
    t.name AS table_name,
    c.name AS column_name,
    ty.name AS data_type
FROM sys.columns c
JOIN sys.tables t ON c.object_id = t.object_id
JOIN sys.schemas s ON t.schema_id = s.schema_id
JOIN sys.types ty 
    ON c.user_type_id = ty.user_type_id 
   AND c.system_type_id = ty.system_type_id
WHERE 
    LOWER(c.name) LIKE '%aumphur%'
 OR LOWER(c.name) LIKE '%amphur%'
 OR LOWER(c.name) LIKE '%district%'
ORDER BY s.name, t.name;
```

### Tambon 

```
SELECT 
    s.name AS schema_name,
    t.name AS table_name,
    c.name AS column_name,
    ty.name AS data_type
FROM sys.columns c
JOIN sys.tables t ON c.object_id = t.object_id
JOIN sys.schemas s ON t.schema_id = s.schema_id
JOIN sys.types ty 
    ON c.user_type_id = ty.user_type_id 
   AND c.system_type_id = ty.system_type_id
WHERE 
    LOWER(c.name) LIKE '%tumbon%'
 OR LOWER(c.name) LIKE '%tambon%'
 OR LOWER(c.name) LIKE '%subdistrict%'
ORDER BY s.name, t.name;
```

***17-03-2026***

| schema_name | table_name                      | column_name    | data_type | การดำเนินการ |
|---|---|---|---|---|
| dbo        | MasterAmphur                       | AmphurCode    | varchar  | ไม่ต้องทำอะไร |
| dbo        | MasterTambon                       | AmphurId      | int      | ไม่ต้องทำอะไร |
| dbo        | RequisitionParticipant             | DistrictId    | varchar  | 🔴 ลบฟิลด์ |
| dbo        | RequisitionParticipant             | SubDistrictId | varchar  | 🔴 ลบฟิลด์ |
| dbo        | RequisitionParticipantAddress      | AumphurId     | int      | 🟡 เปลี่ยนเป็น AmphurId |
| dbo        | RequisitionParticipantAddress      | AumphurName   | nvarchar | 🟡 เปลี่ยนเป็น AmphurName |
| dbo        | MappingPlacePlaceAddress           | AumphurId     | int      | 🟡 เปลี่ยนเป็น AmphurId |
| dbo        | MappingPlacePlaceAddress           | AumphurName   | nvarchar | 🟡 เปลี่ยนเป็น AmphurName |
| dbo        | LicenseParticipantAddress          | AumphurId     | int      | ยังไม่ต้องเปลี่ยนตอนนี้ |
| dbo        | LicenseParticipantAddress          | AumphurName   | nvarchar | ยังไม่ต้องเปลี่ยนตอนนี้ |

| schema_name | table_name                      | column_name    | data_type | การดำเนินการ |
|---|---|---|---|---|
| dbo        | MasterTambon                     | TambonCode    | varchar  | ไม่ต้องทำอะไร |
| dbo        | RequisitionParticipant           | SubDistrictId | varchar  | 🟡 เปลี่ยนเป็น TambonId |
| dbo        | RequisitionParticipantAddress    | TambonId      | int      | ไม่ต้องทำอะไร |
| dbo        | RequisitionParticipantAddress    | TambonName    | nvarchar | ไม่ต้องทำอะไร |
| dbo        | MappingPlacePlaceAddress         | TambonId      | int      | ไม่ต้องทำอะไร |
| dbo        | MappingPlacePlaceAddress         | TambonName    | nvarchar | ไม่ต้องทำอะไร |
| dbo        | LicenseParticipantAddress        | TambonId      | int      | ไม่ต้องทำอะไร |
| dbo        | LicenseParticipantAddress        | TambonName    | nvarchar | ไม่ต้องทำอะไร |

```
Aumphur ❌
District ❌
Amphur ✅

SubDistrict ❌
Tambon ✅
```